project_id = "${PROJECT}"
project_region = "${REGION}"
project_zone = "${ZONE}"

db_access_sa_name = "${DB_ACCESS_SA}"
gke-cluster-name = "${GKE_CLUSTER_NAME}"

source-repository-name = "cloud-x-demo"
app-name = "${APP_NAME}"
cloud-build-name = "cloud-x-demo-ci-cd"
cloud-sql-instance-name = "cloud-x-demo-instance2"